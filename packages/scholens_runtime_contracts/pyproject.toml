[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "scholens-runtime-contracts"
version = "0.1.0"
requires-python = ">=3.12"
description = "Shared validation for Scholens managed runtime endpoints"
readme = "README.md"

[tool.hatch.build.targets.wheel]
packages = ["src/scholens_runtime_contracts"]
