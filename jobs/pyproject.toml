[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[project]
name = "scholens-jobs"
version = "0.1.0"
requires-python = ">= 3.12"
description = "An Application to Run Jobs for the Scholens project"
readme = "README.md"
dependencies = [
    "celery[auth,librabbitmq,msgpack,redis]>=5.6.2",
    "requests>=2.33.0",
    "fastapi>=0.115",
    "uvicorn>=0.34",
    "boto3>=1.34.0",
    "pymupdf>=1.25.5",
    "pymupdf4llm>=1.28.2",
    "markitdown[pdf]>=0.1.7",
    "pillow>=12.2.0",
    "pydantic>=2.0.0",
    "python-dotenv>=1.2.2",
    "psutil>=5.9.0",
    "redis>=5.2.0",
    "httpx>=0.28.0",
    "openai>=2.0.0",
    "posthog>=6.1.0",
    "scholens-observability",
    "opentelemetry-instrumentation-fastapi>=0.50b0,<1",
    "opentelemetry-instrumentation-httpx>=0.50b0,<1",
    "opentelemetry-instrumentation-requests>=0.50b0,<1",
    "opentelemetry-instrumentation-redis>=0.50b0,<1",
    "opentelemetry-instrumentation-celery>=0.50b0,<1",
]

[tool.uv.sources]
scholens-observability = { path = "../packages/scholens_observability" }

[project.scripts]
start = "src:start"

[tool.uv]
constraint-dependencies = [
    "tornado>=6.5.7",
    "pyasn1>=0.6.3",
    "cryptography>=46.0.5",
    "urllib3>=2.7.0",
    "starlette>=1.3.1",
    "idna>=3.15",
]

[dependency-groups]
dev = [
    "boto3-stubs[s3]==1.38.42",
    "celery-stubs>=0.1.3",
    "flower>=2.0.1",
    "mypy>=1.16",
    "pymupdf-stubs==1.28.0",
    "pytest>=8",
    "ruff>=0.5",
    "types-psutil>=7.0.0.20250601",
    "types-requests>=2.32.4.20250611",
]

[tool.mypy]
check_untyped_defs = true

[[tool.mypy.overrides]]
module = ["openai", "openai.*"]
follow_imports = "skip"

[[tool.mypy.overrides]]
module = ["pymupdf4llm", "pymupdf4llm.*", "markitdown", "markitdown.*"]
ignore_missing_imports = true
